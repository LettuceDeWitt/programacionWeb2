// and
var logico = 0 && 0; 
console.log (logico);

var logico = 0 && 1; 
console.log (logico);

var logico = 1 && 0; 
console.log (logico);

var logico = 1 && 1; 
console.log (logico);

// or 
var logico = 0 || 0; 
console.log (logico);

var logico = 0 || 1; 
console.log (logico);

var logico = 1 || 0; 
console.log (logico);

var logico = 1 || 1; 
console.log (logico);
